import React, { Component } from 'react';
import { Button } from 'antd';

export default class Workbench extends Component {
    render() {
        return (
            <div><Button type="primary">antd 主题测试</Button></div>
        );
    }
}
